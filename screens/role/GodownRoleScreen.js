import React from "react";
import { View, StyleSheet, Text } from "react-native";

const GodownRoleScreen = (props) => {
  return (
    <View>
      <Text>Inside Godown Role</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default GodownRoleScreen;
