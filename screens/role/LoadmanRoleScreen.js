import React from "react";
import { View, StyleSheet, Text } from "react-native";

const LoadmanRoleScreen = (props) => {
  return (
    <View>
      <Text>Inside Loadman Role</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default LoadmanRoleScreen;
